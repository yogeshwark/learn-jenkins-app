# Use a specific, stable base image
FROM docker:24.0.5-dind-rootless

# Install AWS CLI and other required dependencies
RUN apk update && apk add --no-cache \
    python3 \
    py3-pip \
    bash \
    curl \
    docker-cli \
    docker-compose

RUN pip install awscli
